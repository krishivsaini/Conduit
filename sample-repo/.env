# FAKE secrets — deny-list demo fixture ONLY. Not real credentials.
# The Conduit server must NEVER expose this file (read/search/tree). See §8.
# NOTE: values are deliberately NOT in any real provider key format, so secret
# scanners don't flag this fixture. The point is only that the FILE exists and
# the server's deny-list refuses it — the contents are irrelevant.
LEDGER_DB_PASSWORD=FAKE_do_not_use_totally_not_real
PAYMENT_PROVIDER_KEY=FAKE_payment_key_denylist_demo_not_real
JWT_SIGNING_SECRET=FAKE_signing_secret_for_denylist_demo
