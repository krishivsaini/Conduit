#!/usr/bin/env bash
# Scripted 90-second demo for the Loom (§14.6). Three beats:
#   1. Security is real   — a path-traversal attempt is refused live; .env denied
#   2. Discovery is real  — the assistant discovers tools and answers a question
#   3. Model-agnostic     — swap LLM_PROVIDER gemini -> ollama; still works
#
# Filled in on Day 14 once the host CLI exists.
set -euo pipefail

echo "Conduit demo — not yet implemented (Day 14)."
echo "Will show: traversal refused -> dynamic discovery -> provider swap."
exit 0
